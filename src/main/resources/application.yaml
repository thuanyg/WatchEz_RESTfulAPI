server:
  port: 8080
  servlet:
    context-path: /watchez

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/watchez_db"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
